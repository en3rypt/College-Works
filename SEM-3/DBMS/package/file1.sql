CREATE TABLE PASSENGERS(
    passenger_id int,
    passenger_name varchar(30),
    passenger_mobile int,
    passenger_email varchar(30),
    passenger_username varchar(30),
    passenger_password varchar(30),
    Primary key(passenger_id,passenger_username)
    
);